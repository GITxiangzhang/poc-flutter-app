import 'package:flutter/material.dart';
class CaremaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(
        title: Text('CAREMA'),
      ),
      body:Center(
        child: Text('CAREMA'),
      )
    );
  }
}